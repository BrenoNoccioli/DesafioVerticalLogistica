spring:
  application:
    name: desafioverticallogistica
  context-path: /vertical-logistica

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/vertical_logistica
    username: root
    password: root
  jpa:
  show-sql: false
  hibernate:
    ddl-auto: update
  properties:
    hibernate:
      dialect: org.hibernate.dialect.MySQL8Dialect